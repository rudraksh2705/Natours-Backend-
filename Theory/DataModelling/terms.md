data modelling -> in real world scanerio, there is unstructured data , it is then converted to structured

type of relationship between data->

1. 1:1
2. 1:many->(few , many ,ton)
3. many : many

Referenced vs Embedding
Reference-> it's easier to query each document on its own , we need 2 queries to get data from referenced
Embedding-> we can get all the info in one query , impossible to query the embedded document on its own
